services:
  web:
    build: .
    ports:
      - "8080:5000"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      FLASK_ENV: development
      FLASK_DEBUG: "1"
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
